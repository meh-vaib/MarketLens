# DEPRECATED - superseded by ci.yml
# Safe to delete this file. Disabled placeholder; manual dispatch only.
name: test-DEPRECATED

on:
  workflow_dispatch:

jobs:
  notice:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Deprecated. Use ci.yml. You can delete test.yml."
