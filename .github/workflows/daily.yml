# DEPRECATED - superseded by daily-report.yml
# Safe to delete this file. Disabled placeholder; manual dispatch only.
name: daily-DEPRECATED

on:
  workflow_dispatch:

jobs:
  notice:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Deprecated. Use daily-report.yml. You can delete daily.yml."
